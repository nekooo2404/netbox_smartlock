# netbox-smartlock

Python package for the `netbox_smartlock` NetBox plugin.

This package is installed into the custom NetBox Docker image from the repository root via:

```text
netbox-docker/Dockerfile.plugin
```

For setup, development workflow, and runtime instructions, see the repository root `README.md`.
